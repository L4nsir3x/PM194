const { restar } = require('./utils');

// Pruebas con la función restar
console.log(restar(10, 5)); // Resultado esperado: 5
console.log(restar(20, 8)); // Resultado esperado: 12
console.log(restar(0, 7));  // Resultado esperado: -7
console.log(restar(15, 15)); // Resultado esperado: 0